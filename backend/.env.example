# ⚠️ IMPORTANT: Replace the placeholder values below with your actual credentials

# Server Port
PORT=5000

# MongoDB Connection String
# Get this from MongoDB Atlas:
# 1. Go to https://cloud.mongodb.com
# 2. Click "Connect" on your cluster
# 3. Choose "Connect your application"
# 4. Copy the connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGO_URI=your_mongodb_connection_string_here

# JWT Secret (use a random string - you can generate one at https://randomkeygen.com/)
# Example: a8f5f167f44f4964e6c998dee827110c
JWT_SECRET=your_random_jwt_secret_here

# Environment
NODE_ENV=production

# Frontend URL (your Vercel deployment URL)
# Make sure this matches your actual Vercel URL exactly
CLIENT_URL=https://syncspace-realtime-collaboration-pl.vercel.app

# Google OAuth
GOOGLE_CALLBACK_URL=https://syncspace-backend-tfcc.onrender.com/api/auth/google/callback

